// const form = document.querySelector("form");

// form.addEventListener('submit', (e) => {
//     e.preventDefault();
//     const fd = new FormData(form);
//     const obj = Object.fromEntries;
// })

const robole = ["mietek", "ruben", "dimitr"];

for(const robol of robole){
    console.log(robol);
}